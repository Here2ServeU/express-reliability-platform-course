apiVersion: constraints.gatekeeper.sh/v1beta1
kind: RequireNsLabels
metadata:
  name: require-ns-labels
spec:
  match:
    kinds:
      - apiGroups: ["*"]
        kinds: ["Namespace"]
  enforcementAction: deny
